#!/usr/bin/env bash
# This script displays the PID and process name of processes containing the word "bash".

# Use pgrep to find processes by name and display PID along with process name.
pgrep -af bash
