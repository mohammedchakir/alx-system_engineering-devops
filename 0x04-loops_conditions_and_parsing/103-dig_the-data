#!/usr/bin/env bash
# Script to group visitors by IP and HTTP status code, displaying occurrence number, IP, and HTTP code

# Use awk to extract and format IP and HTTP status code, then sort and count occurrences
awk '{print $1, $9}' apache-access.log | sort | uniq -c | sort -nr | awk '{print $1, $2, $3}'
